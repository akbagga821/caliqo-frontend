{
  "name": "caliqo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "node scripts/generate-favicon.mjs && node scripts/generate-og-image.mjs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vite": "^6.0.1"
  }
}
